import { type StartTypingInput } from '../schema';

export async function stopTyping(input: StartTypingInput): Promise<void> {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is removing typing indicators when user stops typing.
  // Should clean up the typing indicator from database and emit real-time events.
  return Promise.resolve();
}